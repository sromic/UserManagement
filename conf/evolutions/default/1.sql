# --- !Ups

create table "USER" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, "FIRSTNAME" VARCHAR NOT NULL, "LASTNAME" VARCHAR NOT NULL);

# --- !Downs

drop table "USER";